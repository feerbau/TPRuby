<p id="notice"><%= notice %></p>



<div class="container">
  <h1>Books</h1>
<table class="table table-bordered border-danger table-sm">
  <thead class="table-dark">
    <tr>
      <th scope="col">Title</th>
      <th scope="col" colspan="3">Notas</th>
      <th scope="col" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <th><%= book.title %></th>
        <th><%= book.notes.count %></th>
        <th><%= link_to 'Show', book %></th>
        <th><%= link_to 'Edit', edit_book_path(book) %></th>
        <th><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></th>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path, class:"btn btn-secondary" %>
</div>